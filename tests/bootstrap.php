<?php

    #require_once '../config/config.php';

    #foreach ($dConfig['includes'] AS $file) {
    #    require_once $dConfig['paths']['base_path'] . $dConfig['paths']['classes'] . $file;
    #}
